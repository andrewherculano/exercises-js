const double = function (number) {
    return number * 2
}

const resultDouble = double(5)

const messageValue = function(value) {
    return `O dobro do numerio inserido é: ${value}`
}
console.log(messageValue(resultDouble))